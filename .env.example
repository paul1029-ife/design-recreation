# Absolute origin for canonical URLs, OG images, the sitemap, and the
# shadcn registry URLs printed on each pattern page. Falls back to
# http://localhost:3000 when unset, which is correct for local development.
NEXT_PUBLIC_SITE_URL=https://design-recreation.vercel.app
